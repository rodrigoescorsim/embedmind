# Normalize everything to LF in the repo and working tree — the CI matrix
# includes Linux/macOS and rustfmt assumes LF.
* text=auto eol=lf

# Binary assets (embedding models, demo GIFs) — never normalize
*.onnx binary
*.gif binary
*.png binary
*.mind binary
