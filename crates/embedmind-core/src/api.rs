//! Public API of the engine: `Store`, `Memory`, `Query`.
//!
//! This is the only module the shells (`embedmind-mcp`, `embedmind-cli`) and
//! future bindings are allowed to depend on. Implemented in M1 items 1.2–1.5;
//! data model in `DESIGN.md` §3.2.
